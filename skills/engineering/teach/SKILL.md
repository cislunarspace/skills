---
name: teach
description: 在当前工作区内教用户一项新技能或一个新概念，跨多次会话持续推进。当用户说"teach"、"教我"、"带我学"，或提出想学某个主题时使用。
disable-model-invocation: true
argument-hint: "你想学什么？"
---

用户要你教他一样东西。这是一个有状态的长任务——他打算跨多个会话把这个主题学下去。

## 教学工作区

把当前目录当作教学工作区。用户的学习状态记录在以下文件里：

- `MISSION.md`：记录用户对这个主题感兴趣的**原因**。所有教学都要回到这个原点。格式见 [references/MISSION-FORMAT.md](references/MISSION-FORMAT.md)。
- `./reference/*.html`：参考材料目录。这是从课程里提炼出来的压缩知识——速查表、算法参考、语法、瑜伽体式、术语表。它们是知识的原始单元。每份文档都应当美观、便于打印，专为快速查阅而设计。
- `RESOURCES.md`：可用以夯实教学、获取知识与智慧的资源清单。格式见 [references/RESOURCES-FORMAT.md](references/RESOURCES-FORMAT.md)。
- `./learning-records/*.md`：学习记录目录，记录用户已经掌握了什么。它近似软件开发里的架构决策记录（ADR）——记下非显然的教训和关键洞见，供日后修订或驱动后续会话。它们用来推算用户的最近发展区。文件命名 `0001-<dash-case-name>.md`，编号每次递增。格式见 [references/LEARNING-RECORD-FORMAT.md](references/LEARNING-RECORD-FORMAT.md)。
- `./lessons/*.html`：课程目录。一节**课**（lesson）是一个自包含的 HTML 产出，教一个紧扣任务、范围收得很紧的点。这是本工作区的核心教学单元。
- `./assets/*`：跨课程复用的**组件**。见 [Assets](#assets) 一节。
- `NOTES.md`：草稿本，记用户的偏好和工作中的零散笔记。

## 教学理念

要学到深处，用户需要三样东西：

- **知识**：从高质量、高可信度的资源中获取。
- **技能**：你基于这些知识，设计高度相关的交互式课程来培养。
- **智慧**：来自和其他学习者、实践者的互动。

在 `RESOURCES.md` 还不够充实之前，你的首要任务是去找高质量的资源，帮用户获取知识。永远不要依赖自己的参数化记忆。

有些主题偏技能、有些偏知识。理论物理更偏知识，瑜伽更偏技能。

### 流利度 vs 储存强度

小心区分两类学习：

- **流利度**（fluency strength）：当下从记忆里调取知识的能力。
- **储存强度**（storage strength）：知识的长期留存。

流利度会让用户产生"我已经会了"的错觉，但储存强度才是真正的目标。设计课程时要通过**合意困难**（desirable difficulty）来构建长期留存：

- **提取练习**（retrieval practice）：从记忆里回想。
- **间隔**（spacing）：把练习分散到不同时间。
- **交错**（interleaving）：把相关但不同的主题混在一起练（仅限技能练习）。

## 课程

课程是你主要的产出——知识与技能借由它抵达用户。每节课是一个自包含的 HTML 文件，写到 `./lessons/` 下，命名 `0001-<dash-case-name>.html`，编号每次递增。

课程要**美观**——排版与版式干净、易读，因为用户日后还会回来复习。想着 Tufte 的风格。

课程要短，能很快做完。学习者的工作记忆很小，要留在它的容量之内。但每节课都要给用户一个可以继续往上搭的、实实在在的小成就。它要直接扣住任务，落在用户的最近发展区里。

可能的话，用 CLI 命令为用户打开课程文件。

每节课都要通过 HTML 锚点链接到其他课程和参考文档。

每节课都要推荐一份**一手资料**让用户去读或看，是你在这个主题上找到的质量最高、最可信的资源。

每节课都要提醒用户：有不明白的地方就向 agent 追问。agent 是他的老师，任何不清楚的地方都能帮。

## Assets

课程由可复用的**组件**搭成，存放在 `./assets/`：样式表、测验小部件、模拟器、绘图辅助——任何一节未来的课可能用得上的东西。

复用是默认，不是例外。动笔写一节课前，先读 `./assets/`，从已有的组件搭起。当一节课需要某个新的、可复用的东西时，把它写成 `./assets/` 里的组件并链接过去——绝不要内联一段未来课程会重复的代码。

每个工作区的第一个组件都是一份共享样式表：每节课都链接它，这样这些课看起来像一门成体系的课，而不是一堆一次性页面。工作区越长，组件库也应当随之增长。

## 任务

每节课都要扣住任务——也就是用户想学这个主题的原因。

如果用户说不清任务，或 `MISSION.md` 还没写，你的第一件事就是向用户追问：他为什么要学这个。

不搞清楚任务，知识获取就失去了现实目标的锚。课程会变得太抽象，你也没法判断用户下一步该学什么。

任务会随着用户的技能和知识增长而变化，这很正常——更新 `MISSION.md`，并写一条学习记录记下这次变化。**改动任务前要和用户确认。**

## 最近发展区

每节课，用户都应当觉得难度"恰到好处"——刚好够挑战。

用户可能明确指定想学的东西。如果没有，按下面的方式推算他的最近发展区：

- 读他的 `learning-records`。
- 根据他的任务，判断该教他什么。
- 教落在最近发展区内、最相关的那一样。

## 知识

课程应当围绕一个用户即将掌握的**技能**来设计。课内的知识只放掌握这个技能所必需的部分。先讲知识，再通过交互式的反馈循环让用户练习技能。

知识要先从可信的资源里采集。用 `RESOURCES.md` 追踪它们。课程里要遍布**引用**——把每一个论断都链接到外部资源来支撑。这能提升课程的可信度。

获取知识时，**难度是敌人**——它吃掉你用来理解的工作记忆。

## 技能

如果知识关乎获取，技能就关乎持久与灵活。让知识真正留下来。

技能获取上，**难度是工具**——费力的提取才构建储存强度。技能通过交互式课程来教。手头有几样工具：

- 用小测验和轻度浏览器内任务的交互式课程。
- 带用户走一组现实世界步骤的课程（比如一组瑜伽体式）。

每一种都要建立在**反馈循环**之上：用户就自己的表现得到反馈。这个循环要尽量紧——立即反馈，最好自动。

测验的每个答案字数应当完全一样（可能的话字符数也一样）。不要用格式给用户任何答案线索。

## 获取智慧

智慧来自真正的现实世界互动——在学习环境之外检验自己的技能。

当用户提出一个看起来需要智慧的问题时，你的默认姿态是先尝试作答，但最终把它委托给一个**社群**。

社群是一个（线上或线下）让用户能在真实世界中检验技能的地方。可能是论坛、subreddit、现实中的课程（预算允许的话）、或本地的兴趣小组。

你应当去找用户可以加入的高声誉社群。如果用户明确表示不想加入社群，尊重他。

## 参考文档

创建课程的同时，你也应当创建参考文档。课程可以引用这些文档——它们用来沉淀跨课程复用的原始知识单元。

课程很少会被回头翻看；参考文档会。它们应当是课程的压缩精华，专为快速查阅设计。

有些主题天然适合做参考：

- 编程的语法和代码片段
- 流程的算法和流程图
- 瑜伽的体式和序列
- 健身的动作和训练计划
- 任何自有其一套术语的主题的**术语表**

术语表尤其重要。一旦建好，每节课都要遵守它的用语。格式见 [references/GLOSSARY-FORMAT.md](references/GLOSSARY-FORMAT.md)。

## `NOTES.md`

用户有时会表达他对教学方式的偏好，或提醒你某些需要注意的事。这里就是记录这些偏好的地方，便于你在设计课程或与他协作时回头查阅。
