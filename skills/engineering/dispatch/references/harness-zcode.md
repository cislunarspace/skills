# zcode Harness 适配（预留）

dispatch 在 zcode 里跑时的具体落地方式。

> **本文件待补**。zcode 的 Agent 调度工具、subagent 类型、是否支持多阶段编排脚本、ultracode 等价模式——以上信息尚未确认。等用户补全。

临时做法：在 zcode 里跑 dispatch 时，按 SKILL.md 抽象行为自行映射到 zcode 的可用工具；如果碰到工具能力不够的地方，停下来问用户。